services:
  - type: web
    name: communitymarket
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    disk:
      name: db-data
      mountPath: /opt/render/project/data
      sizeGB: 1
